# FinOps Starter — .env example (v1.4.5)
# Copy to `.env` and adjust for your environment.

# ---- dbt / runtime ----
DBT_TARGET=demo
DBT_THREADS=4

# ---- app flags ----
DEMO_MODE=true
DEV_MODE=false                 # when true, Diagnostics expander opens by default
ENABLE_PRO_PACK=false          # set true ONLY after installing the Pro package

# Optional: where Pro models live (if different from Starter)
PRO_DATABASE=                  # e.g., FINOPS_PRO
PRO_SCHEMA=                    # e.g., MARTS

# Heuristic & pricing defaults
ACTIVITY_FLOOR_SECONDS=60
CREDIT_PRICE_USD=3

# ---- Snowflake connection (leave blank for demo) ----
SNOWFLAKE_ACCOUNT=
SNOWFLAKE_USER=
SNOWFLAKE_PASSWORD=
SNOWFLAKE_ROLE=
SNOWFLAKE_WAREHOUSE=
SNOWFLAKE_DATABASE=            # e.g., FINOPS
SNOWFLAKE_SCHEMA=              # e.g., MARTS

# ---- URLs ----
FINOPS_DOCS_URL=https://mcgrath-dylan.github.io/finops-dbt/
FINOPS_REPO_URL=https://github.com/mcgrath-dylan/finops-dbt
